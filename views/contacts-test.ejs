<!-- views/contact-test.ejs -->

<% contacts.forEach(contact => { %>

  <div>
    <p>Name: <%= contact.name %></p>
    <p>Email: <%= contact.email %></p>
    <p>Subject: <%= contact.subject %></p>
    <p>Message: <%= contact.message %></p>
  </div>

<% }) %>
